# ============================================================================
# Database Configuration
# ============================================================================
DATABASE_URL="postgresql://mealplanner:development@localhost:5432/meal_planner"

# ============================================================================
# Redis Configuration
# ============================================================================
# Option 1: Use REDIS_URL (supports username/password in URL)
# Format: redis://[username[:password]@]host:port or rediss://[username[:password]@]host:port (TLS)
REDIS_URL="redis://localhost:6379"

# Option 2: Use individual environment variables
REDIS_HOST="localhost"
REDIS_PORT="6379"
# REDIS_USERNAME="default"              # Uncomment if Redis requires username (Redis 6+)
# REDIS_PASSWORD="your-redis-password"  # Uncomment if Redis requires authentication

# ============================================================================
# NextAuth.js Configuration
# ============================================================================
# Generate a secret with: openssl rand -base64 32
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="your-secret-here"

# ============================================================================
# Anthropic Claude API
# ============================================================================
ANTHROPIC_API_KEY="your-anthropic-api-key"
# Note: Claude model version is defined in packages/core/src/constants.ts

# ============================================================================
# Email Configuration (Gmail)
# ============================================================================
GMAIL_USER="your-email@gmail.com"
GMAIL_APP_PASSWORD="your-gmail-app-password"

# ============================================================================
# Environment
# ============================================================================
NODE_ENV="development"

# ============================================================================
# Testing Configuration
# ============================================================================
# For testing, copy this file to .env.test and adjust the following:
# - DATABASE_URL: Use test database (e.g., port 5433, database meal_planner_test)
# - REDIS_URL: Use test Redis instance (e.g., port 6380)
# - NEXTAUTH_URL: Use test port (e.g., http://localhost:3001)
# - NEXTAUTH_SECRET: Use a test-specific secret
# - NODE_ENV: Set to "test"
# See .env.test for a complete example

# Mock Mode: Enable mock data for testing/development without external dependencies
# Set to "true" to use Mock Service Worker handlers instead of real API calls
MOCK_MODE="false"
